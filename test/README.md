# Test suite

Smart contrats test suite.

# How to run

Run a node in a tab with:

```bash
make node
```

and in another tab run:

```bash
make test
```

or

```bash
make test-coverage
```

Other options are:

```bash
make build-size
make test-gas
```